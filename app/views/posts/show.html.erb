<h1> <%= @post.title %> </h1>
<p><%= @post.description %></p>

<p><%= link_to "Edit Post", edit_post_path(@post) %> |
  <%= link_to "Delete Demand", post_path(@post), method: :delete, data: { confirm: "Are you sure?" } %></p>
